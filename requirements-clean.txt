# Consolidated requirements for AI Rockfall Prediction System (cleaned)

# Core backend API & async
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database & ORM
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1
motor==3.1.1

# Redis & Celery
redis==5.0.1
celery==5.3.4

# Data processing
numpy==1.26.2
pandas==2.1.4
scipy==1.11.2
scikit-learn==1.3.2
# Use broad versions for xgboost/lightgbm to improve compatibility with local Python
xgboost>=1.7.0
lightgbm>=3.3.5
joblib==1.3.2

# Geospatial & LiDAR
laspy>=2.0.0
# pdal  # commented out: requires CMake/MSVC or conda; will block pip install on Windows without build tools
rasterio
pyproj
pyvista
vtk
open3d
pyntcloud

# Computer vision & image
opencv-python
scikit-image
Pillow

# Visualization & notebooks
matplotlib
seaborn
plotly
plotly-express
ipywidgets
jupyter
papermill
nbformat

# ML explainability
shap

# Storage & file handling
minio
boto3
python-magic
aiofiles

# HTTP, async clients
requests
httpx
aiohttp

# Monitoring & utilities
psutil
structlog
sentry-sdk[fastapi]
python-dotenv

# Dev & testing
pytest
pytest-asyncio
pytest-cov

# Optional / heavy (not installed by default). If you want GPU-enabled deep learning, install separately:
# torch  # install manually with the appropriate CUDA wheel if needed
# torchvision

# NOTE: Some packages (pdal, vtk, open3d) may require system dependencies on Windows (OS packages). If pip install fails, install system binaries or use conda.
